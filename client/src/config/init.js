import initAxios from '../api/initAxios'

/** initialise runtime configurations
 * 1. initAxios = Axios instance interceptors
 * 2. ToBeDefined
 * */
export default function initialize() {
    initAxios();
}

